export const PROFILE_MODULE = 'profile';

export type {
  CandidateProfile,
  ProfileContact,
  ProfileEducation,
  ProfileExperience,
  ResumeFile,
} from './types';
export { createCandidateProfile } from './types';
